# 项目分析文档

## 一、项目背景与目标

### 1.1 项目背景

该项目是一个面向宠物消费与养宠服务场景的前后端分离系统。从代码结构和业务功能判断，项目并不只是传统商品商城，而是围绕“宠物用品购买 + 用户会员中心 + 宠物档案管理 + 智能推荐 + 售后问答 + 补货提醒”构建的一体化宠物服务平台。

现实场景中，宠物主人往往会遇到以下问题：

- 宠物用品种类多，难以快速找到适合自己宠物的商品
- 宠物粮食、日用品等消耗品需要周期性补货，人工记忆成本高
- 用户下单、收货、查看订单、管理地址等基础电商流程较繁琐
- 不同宠物在年龄、体型、健康状况、过敏情况方面差异明显，普通商城缺少个性化推荐能力
- 用户在选品或使用商品过程中，常常需要咨询常见养宠问题

因此，该项目试图将“商品交易”与“宠物个体化服务”结合起来，提升宠物电商平台的服务深度。

### 1.2 项目目标

结合现有代码实现，可以将项目目标概括为以下几点：

- 搭建一个完整的宠物用品在线购物平台，支持商品浏览、详情查看、购物车、下单、支付流程和订单管理
- 建立会员中心，支持登录注册、身份校验、地址管理、密码修改等基础用户能力
- 建立宠物档案体系，记录宠物品种、生日、体重、健康备注、过敏信息、偏好等核心信息
- 基于宠物档案标签实现个性化商品推荐，提高平台转化率和用户体验
- 结合订单记录与宠物日消耗量，生成宠物消耗品补货提醒，增强用户粘性
- 引入 FAQ 问答和 AI 助手能力，提升平台的智能服务水平

### 1.3 业务价值

从毕业设计答辩角度，可以将项目业务价值总结为：

- 从“卖商品”升级为“卖服务”，体现宠物电商平台的差异化竞争力
- 通过宠物档案与推荐逻辑，提高商品推荐的相关性
- 通过订单与消耗预测，实现主动提醒，增强用户复购
- 通过 AI 问答与客服知识库，降低人工客服压力
- 具备典型电商平台的完整业务闭环，适合作为综合型 Web 系统毕业设计项目

## 二、技术架构

### 2.1 前端技术栈

前端项目目录为 `project-pet`，整体采用 Vue3 生态构建，属于典型的现代单页应用架构。

- 框架层：Vue 3
- 构建工具：Vite
- 路由管理：Vue Router 4
- 状态管理：Pinia
- 状态持久化：pinia-plugin-persistedstate
- HTTP 请求：Axios
- UI 组件库：Element Plus
- 开发语言：以 TypeScript 为主，同时保留少量 JavaScript 文件
- 样式方案：CSS + Less

前端架构特点如下：

- 采用路由分模块组织页面，包括首页、商品详情、购物车、结算、支付、会员中心、登录页等
- 使用 Pinia 管理用户登录状态、购物车状态、分类与热门商品等全局数据
- 通过 Axios 二次封装统一处理请求基地址、Token 注入、全局加载动画和错误提示
- 购物车和用户信息做了持久化存储，提升刷新后的使用连续性
- 使用组件化方式组织首页、会员中心和 AI 聊天面板等功能模块，利于维护与复用

### 2.2 后端技术栈

后端项目目录为 `pet-api`，采用 Node.js + Express 构建 REST 风格接口服务。

- 服务框架：Express 4
- 数据访问：MySQL 原生驱动 mysql
- 缓存与会话：Redis
- 跨域处理：cors
- Cookie 解析：cookie-parser
- 配置管理：dotenv
- 时间处理：dayjs
- 唯一标识生成：uuid

后端架构特点如下：

- 通过 Express Router 按业务拆分接口，形成清晰的模块边界
- 使用中间件 `verifyToken` 统一校验登录态
- 将数据库访问封装为连接池与 Promise 工具，提高复用性
- 使用统一响应封装工具，保证接口返回格式一致
- 在订单创建中使用事务，保证订单主表和明细表写入的一致性
- 提供 AI 流式接口，支持前端以流式方式接收回复内容

### 2.3 数据存储设计

项目主要采用 MySQL 作为核心业务数据存储，承担以下职责：

- 用户信息存储
- 商品、分类、SKU、规格数据存储
- 收货地址、订单、订单明细等交易数据存储
- 宠物档案、FAQ 等扩展业务数据存储

MySQL 承担的是平台“主业务数据层”，适合用于结构化、关系明确的电商场景。

### 2.4 缓存设计

项目中 Redis 主要用于登录态管理，而不是大规模页面缓存。

- 用户登录成功后，后端生成 UUID Token
- 将 Token 与用户信息映射写入 Redis，并设置过期时间
- 后续请求通过 `Authorization: Bearer token` 传递身份信息
- 后端中间件从 Redis 中读取 Token 对应的用户数据，完成鉴权

这种设计的优点是：

- 登录态与业务数据解耦
- Token 可设置有效期，便于统一过期控制
- 后端无须把会话强依赖在内存中，具备更好的可扩展性

### 2.5 整体系统架构总结

系统整体属于典型的三层式前后端分离架构：

- 表现层：Vue3 前端页面与组件
- 业务层：Express 接口、中间件、业务路由、推荐与提醒逻辑
- 数据层：MySQL 持久化存储 + Redis 登录态缓存

若用于答辩表达，可将系统架构概括为：

“前端负责交互展示与状态管理，后端负责业务处理与接口提供，MySQL 负责核心业务数据落库，Redis 负责用户会话缓存，同时通过 AI 接口扩展平台智能服务能力。”

## 三、核心功能模块

### 3.1 首页展示模块

首页由轮播图、分类展示、热门推荐和商品区块组成，承担平台引流与商品曝光功能。

主要作用：

- 展示热门商品和分类信息
- 提升用户首次访问时的浏览效率
- 为后续商品详情与下单转化提供入口

业务流程：

- 前端加载首页组件
- 后端提供轮播/滚动商品数据和分类商品数据
- 页面按分类组织商品列表，并展示部分随机商品，增强页面丰富度

### 3.2 商品浏览与详情模块

该模块覆盖商品列表、商品详情、规格展示等核心购物功能。

主要作用：

- 向用户展示商品基础信息、分类归属和销量信息
- 展示 SKU 价格、库存与规格组合
- 为购物车与下单环节提供商品基础数据

业务流程：

- 用户进入商品详情页
- 后端联查商品、分类、SKU、规格值等表
- 将原始规格数据整合为结构化规格信息返回前端
- 前端完成规格选择和数量操作

### 3.3 购物车与结算模块

购物车模块由前端 Pinia 统一管理，支持本地持久化。

主要作用：

- 保存用户待购商品
- 支持勾选、数量累加、价格统计、全选/单选操作
- 在结算页生成订单确认数据

业务流程：

- 用户将商品加入购物车
- 购物车根据商品 ID 和规格组合判重合并
- 进入结算页后，前端读取已选商品并计算商品总价、运费和应付金额
- 同时加载用户收货地址，完成订单确认

### 3.4 订单管理模块

订单模块是系统的交易核心，包含下单、订单列表查询和订单状态更新。

主要作用：

- 将购物车中的商品转化为结构化订单
- 支持查看历史订单
- 管理待支付、已完成、已取消等订单状态

业务流程：

- 用户提交订单
- 后端先写入 `orders` 主表，再写入 `order_details` 明细表
- 订单写入过程采用事务控制，避免主表成功而明细失败
- 用户在会员中心查看订单时，后端对订单和明细做聚合封装，返回适合前端展示的结构

### 3.5 用户与会员中心模块

会员中心是用户登录后的统一管理入口。

主要作用：

- 登录、注册、登出、登录状态校验
- 个人信息访问
- 密码修改
- 我的订单查看
- 宠物档案管理

业务流程：

- 用户登录成功后获得 Token
- 前端持久化用户状态
- 页面刷新后可通过校验接口恢复登录态
- 会员中心按子路由区分个人中心、订单中心、宠物档案、密码修改等页面

### 3.6 地址管理模块

地址模块用于支撑订单履约，是交易流程中的关键基础能力。

主要作用：

- 查询地址列表
- 新增地址
- 设置默认地址
- 为结算页自动匹配默认地址

业务流程：

- 用户进入结算页时请求地址列表
- 若存在默认地址，则默认选中
- 新增地址时若勾选默认地址，系统会先清除其他默认项，再插入新地址

### 3.7 宠物档案模块

该模块是项目区别于普通电商系统的关键业务创新点。

主要作用：

- 建立宠物个体信息档案
- 记录宠物名称、物种、品种、生日、体重、健康情况、过敏情况和偏好
- 为后续推荐、提醒和个性化服务提供基础数据

业务流程：

- 用户在会员中心新增宠物档案
- 后端根据宠物信息自动生成标签
- 标签写入数据库，用于后续推荐逻辑匹配

当前标签生成规则体现出一定的业务智能性，例如：

- 根据犬类体重识别小型犬
- 根据生日推断幼年宠物
- 根据健康备注识别肠胃敏感类标签

### 3.8 个性化推荐模块

该模块用于在用户浏览商品详情页时，基于当前账号下宠物档案中的标签信息返回相关商品。根据源码核查，系统目前并未实现复杂的个性化推荐算法，而是采用“宠物标签生成 + 商品标签匹配”的轻量级规则推荐方式。

主要作用：
- 根据宠物档案中的标签字段筛选相关商品
- 在商品详情页提供补充性推荐结果，方便用户继续浏览和选购
- 为后续扩展更复杂的推荐算法保留基础数据结构

后端实现逻辑：
- 推荐接口位于后端 pet-api/routes/recommend.js，对外提供 GET /member/recommend/:petId 接口
- 接口首先经过 erifyToken 中间件进行登录校验，该中间件仅负责从 Redis 中读取并校验用户 Token，不负责推荐结果缓存
- 接口根据传入的 petId 查询 pets 表中的 	ags 字段
- 宠物标签来源于 pet-api/routes/pet.js 中的宠物档案创建逻辑
- 在创建宠物档案时，系统依据部分固定规则自动生成标签，例如：犬类且体重小于 10kg 时写入“小型犬”标签；生日在一年内时写入“幼年期”标签；健康备注包含“肠胃”时写入“肠胃敏感”标签
- 推荐接口将宠物标签按逗号拆分后，拼接为多个 products.tags LIKE ? 条件，并通过 OR 方式查询 products 表
- 查询结果使用 LIMIT 6 限制数量后直接返回前端

前端展示逻辑：
- 前端推荐展示位于商品详情页 project-pet/src/views/Detail/DetailView.vue
- 页面加载时，前端先调用 /member/pet 获取当前登录用户的宠物列表
- 当前实现默认取宠物列表中的第一只宠物作为推荐依据，并未在详情页提供显式切换宠物后重新推荐的完整交互
- 获取宠物 id 后，前端继续请求 /member/recommend/:petId
- 返回结果经过字段映射后，以“猜你喜欢”模块展示在商品详情页中，最多展示 6 条记录
- 当用户未登录、没有宠物档案或请求失败时，推荐区域不展示内容

数据处理流程：
- 用户新增宠物档案
- 后端根据宠物种类、体重、生日和健康备注等字段生成标签，并写入 pets.tags
- 前端进入商品详情页后请求当前用户宠物列表
- 系统选取第一只宠物的标签作为推荐条件
- 后端根据宠物标签对 products.tags 执行模糊匹配查询
- 商品结果返回前端并展示为推荐列表

技术说明：
- 当前推荐数据来源于 MySQL 中的 pets 表和 products 表，不涉及独立推荐引擎
- 推荐规则属于静态规则匹配与标签模糊检索，并非协同过滤、画像评分模型或机器学习推荐
- 源码中未发现浏览记录、收藏记录、加购记录或购买记录参与推荐计算的实现
- 源码中未发现专门用于推荐结果缓存的 Redis 逻辑，Redis 仅用于登录态校验
- 因此，本模块更准确的表述应为“基于宠物标签与商品标签匹配的规则推荐功能”，不宜夸大为完整的个性化智能推荐系统
### 3.9 消耗品提醒模块

提醒模块结合订单记录与宠物档案中的日消耗量进行补货预测，是本项目最有场景价值的模块之一。

主要作用：

- 识别最近购买的宠物粮或消耗品
- 估算剩余可使用天数
- 在临近耗尽时提醒用户补货

业务流程：

- 系统读取用户宠物列表
- 查询最近一次相关消耗品订单
- 优先使用订单中记录的预计可用天数
- 若无该字段，则使用“购买数量 ÷ 宠物日消耗量”推算
- 再结合下单时间计算剩余天数，并判断是否需要提醒

该模块体现了项目从“被动购物平台”向“主动服务平台”的设计思路升级。

### 3.10 客服与 AI 助手模块

系统提供了 FAQ 客服问答与 AI 助手两类服务能力。

主要作用：

- 常见问题优先通过知识库快速回复
- 无法命中的问题可转向人工或 AI 问答
- AI 助手支持流式输出，提升交互体验

业务流程：

- 用户输入问题后，客服模块先在 FAQ 表中做关键字匹配
- 若命中则直接返回标准答案
- 若未命中，则返回兜底提示，支持转人工或进一步由 AI 处理
- AI 模块通过后端代理外部模型服务，以 SSE 流式方式持续向前端返回内容

## 四、数据库设计

### 4.1 主要数据表

结合 SQL 文件可以推断系统的主要数据表如下：

- `users`：用户表，保存用户名、密码、邮箱、电话等信息
- `user_addresses`：用户地址表，保存收货人、联系方式、地址及默认地址状态
- `categories`：商品分类表
- `products`：商品表，保存商品名称、图片、分类、描述、标签等信息
- `skus`：库存与价格表，保存具体 SKU 的价格、库存、折扣
- `specifications`：规格项表，例如颜色、尺寸等
- `spec_values`：规格值表，例如红色、大号等
- `sku_spec_values`：SKU 与规格值关联表，用于实现多规格商品模型
- `orders`：订单主表
- `order_details`：订单明细表
- `payment_info`：支付信息表
- `pets`：宠物档案表
- `faq`：客服问答知识库表
- `admins`：后台管理或预留管理角色表

### 4.2 主要表关系

从关系设计上看，该数据库遵循了较清晰的实体拆分原则：

- 一个用户可以拥有多个地址，对应 `users` 与 `user_addresses` 的一对多关系
- 一个用户可以拥有多个订单，对应 `users` 与 `orders` 的一对多关系
- 一个订单可以包含多个订单明细，对应 `orders` 与 `order_details` 的一对多关系
- 一个分类下可以包含多个商品，对应 `categories` 与 `products` 的一对多关系
- 一个商品可以对应多个 SKU，对应 `products` 与 `skus` 的一对多关系
- 一个 SKU 可关联多个规格值，对应 `skus` 与 `spec_values` 通过中间表 `sku_spec_values` 建立多对多关系
- 一个用户可以拥有多个宠物，对应 `users` 与 `pets` 的一对多关系

### 4.3 数据库设计思路总结

数据库设计体现出以下思路：

- 将用户、商品、订单、支付等核心业务实体独立建模，保证结构清晰
- 通过主表与明细表分离方式表达订单结构，便于统计和扩展
- 通过 SKU + 规格值 + 中间表的方式支持多规格商品，是典型电商数据库设计方案
- 将宠物档案单独建表，说明系统具备从“用户中心”向“宠物中心”扩展的能力
- 使用标签字段连接宠物画像与商品推荐逻辑，实现轻量级智能推荐

如果用于毕业答辩，可将数据库设计概括为：

“本系统采用关系型数据库建模方法，围绕用户、商品、订单和宠物档案构建核心数据实体，并通过外键与中间表建立完整业务关系，以满足电商交易与个性化服务的双重需求。”

## 五、技术亮点

### 5.1 前后端分离架构清晰

项目将前端页面渲染与后端接口服务完全分离，结构清晰，便于独立开发、联调与部署，也符合现代 Web 工程实践。

### 5.2 基于 Redis 的登录态管理

系统没有把会话简单保存在浏览器或服务器内存中，而是采用 Redis 存储 Token 与用户映射关系。

亮点在于：

- 支持统一过期控制
- 接口鉴权效率较高
- 为后续分布式部署预留了空间

### 5.3 订单事务保证数据一致性

订单创建采用事务机制，先写订单主表，再写订单明细表，若任一环节出错则回滚。

该实现体现了系统对交易一致性的重视，属于电商系统中的关键工程能力。

### 5.4 宠物标签驱动的个性化推荐

系统当前的推荐能力建立在宠物档案标签与商品标签的直接匹配之上。后端在创建宠物档案时生成少量规则标签，并在商品详情页场景下调用推荐接口，用这些标签对 products.tags 字段进行模糊查询，再返回限定数量的商品结果。
这一设计的价值在于：

- 将宠物档案中的部分字段转化为可直接参与筛选的标签数据
- 以较低实现成本完成轻量级规则推荐
- 为后续扩展用户行为特征、排序策略和更复杂推荐算法保留接口基础
### 5.5 基于订单和消耗量的补货提醒

提醒模块将订单数据与宠物日消耗量结合，计算预计剩余天数并进行预警。

该模块亮点突出在：

- 业务贴近真实养宠场景
- 不是简单消息通知，而是基于业务数据推算
- 有助于提升复购率和平台粘性

### 5.6 AI 流式问答能力

系统接入外部 AI 服务，并通过 SSE 流式返回结果，前端实现边生成边展示。

该能力使项目不仅具备传统电商功能，也具备一定的智能交互能力，在答辩中较容易体现项目创新性。

### 5.7 接口与前端请求的统一封装

无论是后端响应处理，还是前端 Axios 请求拦截、统一报错、全局加载，都体现出较好的工程化意识。

这说明项目不仅关注功能实现，也关注代码可维护性和开发规范。

## 六、技术难点

### 6.1 多规格商品数据整合

难点表现：

- 商品详情并非单表即可完成
- 需要联查商品、分类、SKU、规格项、规格值等多个表
- 原始查询结果较为离散，不适合直接给前端使用

解决方案：

- 后端通过多表关联查询拿到规格原始数据
- 再将规格项按名称聚合，整理成前端可直接渲染的结构化格式

这一过程体现了后端数据组装能力。

### 6.2 订单创建的一致性控制

难点表现：

- 一次下单同时涉及订单主表和多条订单明细
- 如果写入过程中某一步失败，容易造成脏数据

解决方案：

- 使用数据库事务包裹整个下单过程
- 通过提交与回滚机制保证订单数据原子性

这是典型的高价值后端实现点。

### 6.3 登录态校验与跨页面保持

难点表现：

- 用户刷新页面后仍需维持登录状态
- 前端和后端需要配合完成身份校验

解决方案：

- 前端持久化用户状态
- 请求拦截器统一携带 Token
- 后端中间件从 Redis 中读取 Token 对应用户信息
- 前端在页面初始化时调用校验接口恢复状态

该方案保证了用户体验与接口安全的统一。

### 6.4 个性化推荐逻辑落地

难点表现：

- 宠物信息是自然语言和多维属性的混合数据
- 需要将其转换为可用于推荐的结构化标签

解决方案：

- 在宠物档案创建接口中，根据种类、体重、生日和健康备注等字段生成标签
- 在商品表中维护 	ags 字段，作为推荐查询依据
- 在推荐接口中将宠物标签拆分后，使用多个 LIKE 条件对商品标签进行模糊匹配
- 将匹配结果限制为最多 6 条并返回前端展示

这一方案虽然实现成本较低，但业务逻辑清晰、实现路径完整，适合作为毕业设计中的规则推荐实现案例。需要说明的是，该实现并未引入用户行为建模、协同过滤或推荐缓存机制，因此属于轻量级规则推荐，而非复杂的个性化推荐算法系统。
### 6.5 消耗品提醒的估算逻辑

难点表现：

- 需要把历史订单数据与宠物属性数据结合起来计算
- 存在不同数据来源，例如订单中的预计天数、宠物日消耗量等

解决方案：

- 优先使用订单明细中的预计天数
- 若缺失则按“购买量 ÷ 日消耗量”进行估算
- 再结合下单时间计算剩余天数，并据此判断是否提醒

这一逻辑体现了系统将数据分析能力嵌入业务功能之中的思路。

## 七、整体总结

### 7.1 项目定位总结

该项目本质上是一个“宠物电商 + 养宠服务”的综合 Web 系统。它不仅完成了商品展示、购物车、下单、订单管理等基础电商功能，还围绕宠物档案、个性化推荐、消耗品提醒和 AI 问答等功能进行了服务化扩展。

### 7.2 答辩表达建议

如果后续用于毕业答辩 PPT，可以重点突出以下三点：

- 项目不是单纯商城，而是结合宠物画像与智能服务的业务平台
- 系统架构完整，包含前端、后端、数据库、缓存和智能接口扩展
- 既有基础业务闭环，也有推荐、提醒、AI 问答等创新模块

### 7.3 可进一步延伸的优化方向

从答辩提升角度，还可以将项目的后续扩展方向概括为：

- 引入更细粒度的权限与后台管理系统
- 对推荐模块引入协同过滤或画像评分模型
- 对提醒模块引入定时任务与消息推送机制
- 对密码与敏感信息引入加密存储
- 对订单、商品与用户行为增加数据统计分析模块

这些优化方向能够体现项目具备继续演进为完整产品的潜力。
